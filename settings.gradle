pluginManagement {
    repositories {
        gradlePluginPortal() // for Freefair plugins
        mavenCentral()
        google()
    }
}

rootProject.name = 'Azulejo-Master'

include 'launcher', 'launcher-builder', 'launcher-bootstrap', 'creator-tools'
